{
  "name": "bizcopilot",
  "version": "1.0.0",
  "description": "AI-Powered Business Intelligence Copilot",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "start": "cd server && node src/index.js",
    "install:all": "npm install --legacy-peer-deps --include=dev && cd server && npm install --legacy-peer-deps --include=dev && cd ../client && npm install --legacy-peer-deps --include=dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}